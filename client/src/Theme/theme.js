import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#303030',
    },
    secondary: {
      main: '#8f8e8e',
    },
    info: {
      main: '#ffffff',
    },
  },
});

export default theme;
